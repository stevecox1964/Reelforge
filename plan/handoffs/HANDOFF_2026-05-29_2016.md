# Handoff — Per-stage model selection shipped; project renamed to Reelforge; GitHub rename + push pending

**Date:** 2026-05-29 20:16 local
**Branch:** main
**Prior handoff:** [HANDOFF_2026-05-28_0126.md](HANDOFF_2026-05-28_0126.md)

## TL;DR

Two pieces of work are committed locally but **nothing is pushed** — `main` is 2 commits ahead of `origin/main`, and the model-selection feature is still on its own branch. The next session's job is the rename/sync finish line: rename the GitHub repo to `reelforge` (user is doing this manually — no `gh`/token here), merge the feature branch into `main`, update the remote URL, and push.

## What was done this session

- **Committed the v1 refactor baseline** (from the prior handoff's 70-file working tree) as a single snapshot: `178d524` on `main`. Verified `.gitignore` excludes `Projects/*/outputs/**`, `fal_results/**`, and `.studio/` — only metadata JSON/markdown was committed, no media binaries.
- **Built per-stage model selection (feature #2 from `plan/current_plan.md`)** on branch `feat/per-stage-model-selection`, commit `b7a7db4`:
  - New `Python/scripts/pipeline/models.py`: a registry seeded to `.studio/models.json` (gitignored, seeds from code), with `resolve_model(stage, project)`. Precedence: per-project `project.json` `models` block → stage default. Fails loud (KeyError) on unknown stage/model.
  - `generate_storyboards.py`, `generate_clips.py`, `generate_voiceover.py`: dropped hardcoded `DEFAULT_MODEL`; now call `resolve_model(...)`. CLI `--model` still works as an override (defaults to None).
  - `server.py`: added `GET /api/models`, `PUT /api/projects/{name}/models` (validated against registry), and `models` block exposed in project summary/detail. Seeds registry at startup.
  - `web/studio/app.js` + `styles.css`: the static "Model" value in the storyboards/clips gates and the voiceover stage is now a `<select>` populated from the registry; changing it PUTs the choice and persists.
  - Registry seeds **only confirmed-working IDs** (per memory `feedback-fal-model-ids`): `fal-ai/flux/schnell` + `openai/gpt-image-2` (storyboards), `fal-ai/kling-video/v3/standard/image-to-video` (clips), `xai/tts/v1` (voiceover).
  - **Verified** via TestClient: GET catalog ✓, PUT persists ✓, bad stage/model → 400 ✓, override flips storyboards to gpt-image-2 while clips stays default ✓. Test mutations to a real project.json were reverted. NOT verified in a live browser.
- **Renamed the project to "Reelforge"**, commit `edb53fe` on `main`: README title + one-liner ("orchestration engine for short (20–30s) videos"), `pyproject.toml` name `codex-fall-video-gen` → `reelforge`, `uv lock` regenerated (verified: only the rename + alphabetical reorder, no dependency changes).

## In flight

- **Merging `feat/per-stage-model-selection` into `main`.** The plan was: rebase the feature onto `main` (the feature branched from `178d524`, before the rename `edb53fe`, so it doesn't contain the rename), then `git checkout main && git merge --ff-only`. No file overlap between the feature and the rename commit, so the rebase should be conflict-free. **This command was interrupted/rejected and did NOT run — repo is untouched, still on `main` at `edb53fe`, feature branch unchanged at `b7a7db4`.**
- **GitHub repo rename + push** not started. `gh` is not installed and there's no GitHub token in env/.env, so the rename itself must be done by the user in the GitHub web UI (Settings → General → Rename to `reelforge`). GitHub auto-redirects the old URL.

## Decisions locked in this session

- **Commit strategy:** v1 refactor committed as one snapshot (not split into ~5), per user.
- **Provider scope for model selection:** FAL-only first; Replicate/adapter layer deferred to feature #3. Per user.
- **Registry storage:** global catalog in code seeds `.studio/models.json` (gitignored); per-project override in `project.json` `models` block. Stage scripts resolve via `resolve_model()`. (Was already the plan; now implemented.)
- **Project name:** **Reelforge** — human title "Reelforge", package/repo slug `reelforge`. Chosen over shortsmith/montage/maestro. Reason: "reel" + "forge" evokes the staged assembly; "Codex" wrongly implied a code tool.
- **Rename, don't recreate:** keep the existing GitHub repo and rename it (preserves history/redirects) rather than starting a new repo.
- **Rename commit lives on `main`:** it's baseline metadata, independent of the feature.

## Open questions

- **Feature merge style:** rebase-then-ff (planned, gives linear history) vs. a merge commit. Either is fine; nothing is pushed so rebase is safe. User interrupted before this ran — confirm they're OK with the rebase approach, or just do a plain merge.
- **Folder rename timing:** user asked "tell me when to rename the folder." Answer: rename `Codex_Fall_Video_Gen/` on disk **after this session ends** (close editor/terminal so nothing holds the dir, rename, reopen in the new path). Git is indifferent to the folder name; repo name, remote URL, and folder name are all independent.

## Next concrete step

Finish the sync once the user has renamed the repo on GitHub: run `git remote set-url origin https://github.com/stevecox1964/reelforge.git`, then merge the feature into main with `git checkout feat/per-stage-model-selection && git rebase main && git checkout main && git merge --ff-only feat/per-stage-model-selection`, then `git push origin main`. (If the user hasn't renamed on GitHub yet, the push still works against the old URL — the redirect keeps it valid either way.)

## Files touched this session

- **Created:** `Python/scripts/pipeline/models.py` (registry + `resolve_model`); `plan/handoffs/HANDOFF_2026-05-29_2016.md` (this file).
- **Modified (in commit `b7a7db4`, on feature branch):** `Python/scripts/pipeline/generate_storyboards.py`, `generate_clips.py`, `generate_voiceover.py`, `Python/scripts/studio/server.py`, `web/studio/app.js`, `web/studio/styles.css`.
- **Modified (in commit `edb53fe`, on main):** `README.md`, `pyproject.toml`, `uv.lock`.
- **Untracked/runtime (not committed):** `.studio/models.json` (gitignored — seeds from code on server start).

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is: after the user renames the GitHub repo to `reelforge`, run `git remote set-url origin https://github.com/stevecox1964/reelforge.git`, rebase `feat/per-stage-model-selection` onto `main` and ff-merge it, then `git push origin main`.

## Git state at handoff

```
(working tree clean — nothing modified/staged/untracked)
```

Recent commits:

```
edb53fe Rename project to Reelforge
178d524 Studio v1 refactor: Projects/<name>/ layout + new pipeline + UI polish + single run.bat
e48d39e Add project README
d693013 Initial video engine workspace
```

Branches:

```
* main                           edb53fe [origin/main: ahead 2] Rename project to Reelforge
  feat/per-stage-model-selection b7a7db4 Per-stage model selection via UI
origin/main                      e48d39e Add project README   (push pending)
```
