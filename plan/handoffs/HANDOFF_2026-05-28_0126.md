# Handoff — Studio v1 refactor complete; v2 feature roadmap captured in plan/current_plan.md

**Date:** 2026-05-28 01:26 local
**Branch:** main
**Prior handoff:** _None — first handoff for this project_

## TL;DR

The full Docs/MediaGeneration → Projects/<name>/ refactor is **done and working end-to-end** in a fresh studio UI. Nothing is committed yet — 70 files in a working-tree refactor. Next session: review the diff, decide whether to commit as one snapshot or split, then start on **Studio v2 feature work** documented in `plan/current_plan.md` (sidebar reorder is the trivial first step).

## What was done this session

- **Migrated all 7 existing projects** from `Docs/MediaGeneration/{briefs,manifests,recipes,outputs,storyboards}/` into `Projects/<name>/{brief.md, scene_plan.json, recipes/, outputs/, fal_results/, manifest.json, project.json}`. Migration script: `Python/scripts/migrate_projects.py` (idempotent, tolerates missing source files).
- **Rewrote the stage pipeline** under `Python/scripts/pipeline/` — `paths.py`, `status.py`, `fal_runner.py`, `create_project.py`, `generate_storyboards.py`, `generate_clips.py`, `generate_voiceover.py`, `assemble_review.py`, `cost_report.py`. All take `--project <name>` and resolve paths from `Projects/<name>/`. New runner writes media to canonical paths instead of timestamped filenames.
- **Rewrote `Python/scripts/studio/server.py`** for the new layout. Endpoints: `GET/POST /api/projects`, `GET /api/projects/{name}`, `POST /api/projects/{name}/stages/{stage}/{run,approve}`, `POST /api/projects/{name}/clone`, `PUT /api/projects/{name}/scene-plan`, `GET /api/jobs`. Static-serves `/projects/{name}/outputs/`.
- **Polished the vanilla JS studio UI** at `web/studio/{index.html, app.js, styles.css}`:
  - 5-card flow (Concept → Scene Plan → Storyboards → Clips → Complete) wired to new API.
  - Card 5 handles voiceover + review assembly as two sub-stages with their own gating.
  - Clickable progress-track icons jump to any stage's card (auto-expands collapsed cards).
  - Sidebar reordered with **+ New Project** at the top (above the project list).
  - Projects sorted by latest activity (manifest mtime).
  - Card 1 has two modes: editable "create" form for new projects, read-only summary + **Clone Project →** for loaded existing projects.
  - `reattachRunningJobs()` re-attaches the polling spinner after page reload if a job is still queued/running.
  - "Start a New Project" button in Card 5 replaced with **↻ Refresh** — the sidebar's + button is the single source of truth.
- **Collapsed 12 batch files into one `run.bat`** that kills any prior port-8765 listener and orphaned `worker.py` before launching `Python/scripts/studio/run_studio.py` (which spawns the server + worker subprocess as a managed pair).
- **Bug fixed mid-session:** the voiceover stage was broken because I had set `DEFAULT_MODEL = "fal-ai/playai/tts/v3"` (made-up ID). Restored to working `xai/tts/v1`. Verified via FAL pricing API.
- **Wrote the global `/handoff` skill** at `~/.claude/skills/handoff/SKILL.md` — pure markdown, no scripts. Self-designed from observed patterns, not copied from any repo. This handoff is being written via that template manually because the user reboots before they can invoke `/handoff` for the first time.
- **Wrote `plan/current_plan.md`** capturing the four open feature requests with decisions and execution order.

## In flight

- **Nothing mid-edit at the file level.** All file changes are saved.
- **70 files in working tree, 0 commits.** All work since the last commit (`e48d39e Add project README`, on 2026-05-15) is uncommitted. The user has been testing the running app and verifying everything works before commit.
- **Three open questions on the v2 feature plan** (see `plan/current_plan.md` "Open questions"): Replicate auth UX, recipe format on provider switch, usage page refresh policy. Not blocking — can be answered when the relevant step is reached.

## Decisions locked in this session

- **Project layout:** `Projects/<name>/{brief.md, scene_plan.json, recipes/, outputs/, fal_results/, manifest.json, project.json}` at repo root. `Docs/` folder eliminated entirely.
- **Migration approach:** Migrate all 7 existing projects into the new layout (chosen over archive-and-start-fresh).
- **UI tech:** Keep vanilla JS, polish the existing studio (chosen over React rebuild).
- **Refactor scope:** One big refactor on a single branch, commit-per-step (chosen over phased rollout).
- **Doc cleanup:** Delete the stale `Docs/MediaGeneration/*.md` notes (`fal_video_generation_workflow.md` etc.) — `current_plan.md` and the README are the only living docs.
- **Studio runtime state:** SQLite at `.studio/studio.sqlite3` (gitignored). Worker spawned as child subprocess of the studio launcher, managed by `run_studio.py`.
- **Worker preservation:** Kept SQLite + separate worker for restart-safety (validated working in earlier sessions).
- **Paid-stage gating preserved:** Storyboards / clips / voiceover all require explicit approval before the next paid stage. Review assembly is local-only but still gated behind voiceover approval.
- **Default models locked:** `fal-ai/flux/schnell` (storyboards), `fal-ai/kling-video/v3/standard/image-to-video` (clips), `xai/tts/v1` (voiceover). These will move into `.studio/models.json` registry as part of v2.
- **Handoff vs plan distinction:** `plan/current_plan.md` = roadmap (what we're building). `plan/handoffs/HANDOFF_*.md` = per-session diary entries (where we left off). Never overwrite or merge.

## Open questions

These need user input before the relevant v2 step starts. Full context in `plan/current_plan.md`.

- **Replicate auth UX** — When `REPLICATE_API_TOKEN` isn't set, hide Replicate models from the UI, or show them and fail at run time with a clear error?
- **Recipe format on provider switch** — Normalized intent (one shape, adapter translates) or provider-native (regenerate recipes when switching models)?
- **Usage page refresh** — Manual refresh button or auto-recompute on every page load?

## Next concrete step

**Review the uncommitted diff and decide commit strategy.** Run `git diff --stat HEAD` and either:
- (a) Commit everything as one snapshot: `Studio v1 refactor: Projects/<name>/ layout + new pipeline + UI polish + single run.bat`, then start step 1 of v2 (sidebar reorder is already done, so step 2 — `pipeline/models.py` + `.studio/models.json` seeding).
- (b) Split into ~5 logical commits (migration, pipeline, server, UI, batch consolidation). Higher-effort but cleaner history.

Either way, do not start v2 work on top of an uncommitted 70-file working tree.

## Files touched this session

Counts from `git diff --stat HEAD` plus untracked:

- **Modified (6):** `.gitignore`, `Python/scripts/studio/server.py`, `README.md`, `web/studio/app.js`, `web/studio/index.html`, `web/studio/styles.css`.
- **Deleted (~55):** Entire `Docs/MediaGeneration/` tree (briefs, manifests, recipes, storyboards, outputs, templates, stale .md notes, studio.sqlite3, studio_jobs.json). Also 11 old batch files. Also `Python/scripts/media/{fal_generate.py, fal_kandinsky_smoke.py, fetch_fal_result.py}` (replaced by pipeline/).
- **Untracked (created this session):**
  - `Projects/` (the 7 migrated projects + 1 user-tested `short_animated_film_cat_detective`)
  - `Python/scripts/migrate_projects.py`
  - `Python/scripts/pipeline/` (9 modules: __init__, paths, status, fal_runner, create_project, generate_storyboards, generate_clips, generate_voiceover, assemble_review, cost_report)
  - `Python/scripts/studio/run_studio.py`
  - `Python/scripts/studio/worker.py`
  - `plan/current_plan.md`
  - `plan/handoffs/HANDOFF_2026-05-28_0126.md` (this file)
  - `run.bat`
  - `.studio/studio.sqlite3` (gitignored)

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is: review the uncommitted diff with `git diff --stat HEAD` and decide whether to commit the 70-file refactor as one snapshot or split into ~5 logical commits, then start v2 feature work per `plan/current_plan.md`.

## Git state at handoff

```
M  .gitignore
M  Python/scripts/studio/server.py
M  README.md
M  web/studio/app.js
M  web/studio/index.html
M  web/studio/styles.css
D  Docs/MediaGeneration/...  (entire tree, ~55 files)
D  fetch_text_to_video_result.bat, run_fal.bat, run_studio.bat,
    run_text_to_video_now.bat, test_fal_key.bat, test_text_to_video.bat,
    "test_text_to_video - Copy.bat", and 4 others
D  Python/scripts/media/{fal_generate.py, fal_kandinsky_smoke.py, fetch_fal_result.py}
?? Projects/                                       (7 migrated + 1 test project)
?? Python/scripts/migrate_projects.py
?? Python/scripts/pipeline/                        (9 new modules)
?? Python/scripts/studio/run_studio.py
?? Python/scripts/studio/worker.py
?? plan/                                           (current_plan.md + handoffs/)
?? run.bat
```

Recent commits:

```
e48d39e Add project README
d693013 Initial video engine workspace
```
